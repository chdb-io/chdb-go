The zstd-compressed `libchdb` parts for darwin/amd64 are written here by
`scripts/package-engine.sh` and are present only on tagged releases of this
module. This file keeps the directory embeddable when they are absent.
