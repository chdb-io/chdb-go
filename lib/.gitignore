# Engine payloads are generated by scripts/package-engine.sh and are added
# explicitly at release time. Ignoring them keeps a routine commit from
# carrying a few hundred megabytes.
*/data/libchdb.zst.part*
